# =============================================================================
# Campus Ride-Share Platform - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in all values before running the application.
# Never commit your .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Secret key for Flask sessions - generate a strong random key
# Use: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Application display name
APP_NAME=Campus Ride-Share

# Base URL of the application (used for email links)
# In production, this should be your actual domain with HTTPS
APP_URL=http://localhost:5000

# University email domain for registration validation
# Only emails ending with this domain can register
UNIVERSITY_DOMAIN=university.edu

# Price per kilometer for suggested ride pricing (in your local currency)
PRICE_PER_KM=5

# -----------------------------------------------------------------------------
# Database Settings
# -----------------------------------------------------------------------------

# Path to SQLite database file (relative to project root or absolute path)
DATABASE_PATH=rideshare.db

# -----------------------------------------------------------------------------
# OpenAI API Configuration
# -----------------------------------------------------------------------------

# Your OpenAI API key for the chatbot feature
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-key-here

# -----------------------------------------------------------------------------
# Google Maps API Configuration
# -----------------------------------------------------------------------------

# Your Google Maps JavaScript API key
# Get one at: https://console.cloud.google.com/google/maps-apis
# Enable: Maps JavaScript API, Places API, Directions API, Distance Matrix API
GOOGLE_MAPS_API_KEY=your-google-maps-key-here

# -----------------------------------------------------------------------------
# Email (SMTP) Configuration
# -----------------------------------------------------------------------------

# SMTP server hostname
# For Gmail: smtp.gmail.com
# For Outlook: smtp-mail.outlook.com
SMTP_HOST=smtp.gmail.com

# SMTP server port
# For TLS: 587
# For SSL: 465
SMTP_PORT=587

# SMTP authentication username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP authentication password
# For Gmail, use an App Password: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your-app-password-here

# Display name for outgoing emails
EMAIL_FROM_NAME=Campus Ride-Share

# From address for outgoing emails
EMAIL_FROM_ADDRESS=your-email@gmail.com

# -----------------------------------------------------------------------------
# Admin Configuration
# -----------------------------------------------------------------------------

# Email address that will automatically become admin on first registration
# The first user to register with this email gets admin privileges
ADMIN_EMAIL=admin@university.edu
